h1 Listing news

table
  thead
    tr
      th Content
      th User
      th
      th
      th

  tbody
    - @news.each do |news|
      tr
        td = news.content
        td = news.user_id
        td = link_to 'Show', news
        td = link_to 'Edit', edit_news_path(news)
        td = link_to 'Delete', news, data: { confirm: 'Are you sure?' }, method: :delete

br

= link_to 'New News', new_news_path
